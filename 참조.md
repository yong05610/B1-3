# 노코드 자동화 기초: 워크플로우 설계

> **AI 도구 학습 / 학습시간 40시간**  
> 프로젝트 1: 자동화 도구 비교 구현 · 프로젝트 2: 자유 주제 자동화 설계 및 구현  
> 최종 보완일: **2026-08-14**

## 제출물 구성

- `README.md`: 설계, 구현, 비교, 실행 증빙, 운영/확장 설계
- `assets/`: Make·Zapier 구성 화면 및 실제 실행 결과 캡처
- `security_scan.py`: 제출물의 주요 Secret 패턴 자동 검사 스크립트
- `security-scan.txt`: 자동검사 실행 결과
- `노코드_자동화_워크플로우_보고서.pdf`: README의 PDF 제출본

## 평가 보완 사항 반영 요약

| 보완 항목 | 반영 내용 |
|---|---|
| 툴별 구성/실행 직접 증빙 | Make와 Zapier 각각 **구성 화면 + 실행 화면 + 최종 출력**을 증빙 매트릭스로 명시 |
| 동일 워크플로우 유지 근거 | 프로젝트 1의 **입력 표준, 분기 규칙, 핵심 Action, 최종 출력 규격**을 별도 명문화 |
| 단계별 입력/출력 | Trigger부터 최종 Action까지 **샘플 payload** 추가 |
| 비교 근거 심화 | Make vs Zapier 비교표에 **근거/업무 영향** 열 추가 |
| Filter/Router 선택 근거 | 분기 수, 유지보수성, 데이터량 관점에서 선택 기준 추가 |
| 비용/운영 관점 | 무료 플랜, Action 수, task/credit 영향과 운영 사례 추가 |
| 모니터링/재시도/대체경로 | **운영 안정성 설계안** 추가 |
| 모듈화/재사용 | 서브시나리오 분리 기준과 재사용 원칙 추가 |
| 노코드 한계/코딩 확장 | 한계 사례와 **P1~P3 우선순위 실행계획** 추가 |
| 보안 검증 | 이메일 주소가 보이던 캡처를 마스킹하고 Secret 자동검사 결과 첨부 |

---

# 1. 프로젝트 개요

반복 업무를 분석한 뒤 **Trigger, Action, Filter/Router**를 활용해 실제로 동작하는 자동화 워크플로우를 구현하였다.

- **프로젝트 1**: 동일한 업무 요청 자동화를 **Make와 Zapier**에서 각각 구현하고 비교
- **프로젝트 2**: **Make + Google Drive + Google Sheets**를 이용해 업로드 파일을 PDF와 이미지로 자동 분류하고 처리 이력을 기록

## 1.1 핵심 개념

| 개념 | 정의 | 이번 과제의 실제 사례 |
|---|---|---|
| Trigger | 자동화를 시작시키는 이벤트 | 요청접수 시트 새 행, Drive `01_업로드` 새 파일 |
| Action | Trigger 이후 실제 업무를 수행하는 단계 | 시트 행 추가, Gmail 발송, Drive 파일 이동 |
| Filter | 조건을 만족하는 데이터만 다음 단계로 통과 | `우선순위 = 긴급`, `파일명 Ends with .pdf` |
| Router / Paths | 하나의 입력을 조건별 여러 경로로 분기 | 긴급/일반, PDF/이미지 |
| Execution Log | 단계별 입력·출력과 성공/실패를 확인하는 기록 | Make Scenario History, Zapier Zap History |

## 1.2 과제 요구사항 충족 구조

| 요구사항 | 프로젝트 1 | 프로젝트 2 |
|---|---|---|
| Trigger 1개 이상 | Google Sheets 새 행 감지 | Google Drive 새 파일 감지 |
| Action 2개 이상 | 시트 행 추가 + Gmail 알림 | 파일 이동 + Sheets 이력 기록 |
| 조건 분기 | 긴급 / 일반 | PDF / 이미지 |
| 두 분기 실행 증빙 | 긴급 1건 + 일반 1건 | PDF 1건 + 이미지 1건 |
| 사용 도구 | Make + Zapier | Make |

---

# 2. 프로젝트 1 - 자동화 도구 비교 구현

## 2.1 자동화 주제

**업무 요청 자동 분류 및 알림 시스템**

업무 요청이 등록되면 우선순위에 따라 **긴급 / 일반**으로 자동 분류하고, 각 전용 Google Sheets 시트에 저장한 뒤 Gmail 알림을 발송한다.

## 2.2 동일성 유지 규격 - Make와 Zapier가 같은 워크플로우임을 보장

두 도구는 화면 구성과 모듈 이름은 다르지만, 아래 **논리 규격(Logical Specification)**을 동일하게 적용하였다. 도구별 기능명을 먼저 정한 것이 아니라 업무 규칙을 먼저 정의한 뒤 Make와 Zapier에 각각 매핑했다.

### 입력 표준

| 필드 | 형식 | 허용값/예시 | 용도 |
|---|---|---|---|
| 접수시간 | 날짜/시간 | `2026-08-14 12:00` | 요청 발생 시각 |
| 요청자 | Text | `이충훈` | 요청자 식별 |
| 제목 | Text | `MES 서버 접속 장애` | 알림 제목/업무 식별 |
| 우선순위 | Enum | `긴급`, `일반` | **분기 기준** |
| 내용 | Text | `MES 서버 접속 불가` | 상세 업무 내용 |

### 핵심 분기·Action·최종 출력 규격

| 논리 단계 | 긴급 | 일반 | Make 구현 | Zapier 구현 |
|---|---|---|---|---|
| Trigger | 동일한 `요청접수` 신규 행 | 동일 | Watch New Rows | New Spreadsheet Row |
| 분기 규칙 | `우선순위 = 긴급` | `우선순위 = 일반` | Router + Filter | Paths rules |
| 핵심 Action 1 | `긴급처리` 시트 행 추가 | `일반처리` 시트 행 추가 | Add a Row | Create Spreadsheet Row |
| 핵심 Action 2 | 긴급 Gmail 발송 | 일반 Gmail 발송 | Send an Email | Send Email |
| 최종 출력 | 시트 1행 + 이메일 1건 | 시트 1행 + 이메일 1건 | 동일 | 동일 |

### 설계 근거

- **입력 표준을 고정**해 도구 차이가 결과 차이로 이어지지 않도록 했다.
- **분기값을 `긴급/일반` 2개로 제한**해 Make와 Zapier에서 동일한 테스트가 가능하게 했다.
- 각 경로의 핵심 Action을 **시트 기록 + 메일 발송 2개로 고정**하여 비교 대상이 UI 차이가 아니라 동일 업무의 구현 차이가 되도록 했다.
- 최종 성공 기준도 동일하게 **“올바른 처리 시트에 1행 저장 + 해당 종류 이메일 1건 수신”**으로 정의했다.

## 2.3 공통 워크플로우

![프로젝트 1 공통 워크플로우](assets/project1-workflow.png)

```text
Google Sheets 신규 요청
        ↓
     조건 분기
     ↙      ↘
   긴급      일반
    ↓         ↓
전용 시트   전용 시트
행 추가     행 추가
    ↓         ↓
Gmail      Gmail
알림 발송   알림 발송
```

## 2.4 툴별 구성 화면 + 실행 화면 직접 증빙

아래 표는 **구성화면과 실행화면을 같은 항목에서 병기**하여 실제 설정과 실행을 동시에 확인할 수 있도록 정리한 증빙 매트릭스다.

| 도구 | 구성 화면 증빙 | 실행 화면 증빙 | 최종 출력 증빙 |
|---|---|---|---|
| **Make** | 전체 Scenario에서 `Google Sheets → Router → Sheets → Gmail` 연결 확인<br><img src="assets/make-urgent-run.png" width="430"> | 긴급/일반 경로가 실제 실행된 화면<br><img src="assets/make-normal-run.png" width="430"> | Gmail 실제 수신<br><img src="assets/make-urgent-email.png" width="430"> |
| **Zapier** | `New Spreadsheet Row → Paths → Create Row → Send Email` 구조 확인<br><img src="assets/zapier-workflow.png" width="430"> | Path 실행 및 Gmail Action 성공 확인<br><img src="assets/zapier-path-run.png" width="430"> | Gmail 실제 수신<br><img src="assets/zapier-email-normal.png" width="430"> |

> **증빙 판정 기준:** 구성 화면에서 Trigger·분기·Action 연결이 보여야 하고, 실행 화면에서는 실제 경로 실행 또는 Action 성공 상태가 보여야 하며, 최종 출력 화면에서는 이메일 또는 시트 결과가 확인되어야 한다.

### Make 구성 화면 증빙

![Make 구성 화면 - Google Sheets, Router, Sheets, Gmail 연결](assets/make-urgent-run.png)

### Make 실행 결과 증빙

![Make 일반 분기 실제 실행](assets/make-normal-run.png)

![Make 긴급 Gmail 최종 출력](assets/make-urgent-email.png)

### Zapier 구성 화면 증빙

![Zapier 구성 화면 - Trigger, Paths, Sheets, Gmail](assets/zapier-workflow.png)

### Zapier 실행 결과 증빙

![Zapier Path 실제 실행 및 Gmail Action 성공](assets/zapier-path-run.png)

![Zapier 일반 Gmail 최종 출력](assets/zapier-email-normal.png)

## 2.5 Make 구현 및 설정 명세

### 구현 순서

1. `Google Sheets - Watch New Rows`를 Trigger로 추가
2. `Flow Control - Router` 추가
3. 긴급 경로 Filter를 `우선순위 = 긴급`으로 설정
4. `Google Sheets - Add a Row`로 긴급처리 시트 저장
5. `Gmail - Send an Email`로 긴급 알림 발송
6. 일반 경로 Filter를 `우선순위 = 일반`으로 설정
7. 일반처리 시트 저장 + Gmail 일반 알림 연결
8. `Run once`로 긴급/일반 데이터 각각 테스트

### Action 필드 매핑 명세

| 입력(Trigger) | 긴급처리/일반처리 시트 | Gmail 본문 사용 |
|---|---|---|
| 접수시간 | 접수시간 | 필요 시 접수시간 |
| 요청자 | 요청자 | `요청자:` 뒤에 매핑 |
| 제목 | 제목 | 메일 제목 + 본문 |
| 우선순위 | 우선순위 | `우선순위:` 뒤에 매핑 |
| 내용 | 내용 | `내용:` 뒤에 매핑 |

**Gmail 제목 규격**

```text
긴급: [긴급 업무요청] {제목}
일반: [일반 업무요청] {제목}
```

## 2.6 Zapier 구현 및 설정 명세

### 구현 순서

1. `Google Sheets - New Spreadsheet Row`를 Trigger로 설정
2. `Paths by Zapier` 추가
3. Path A 규칙: `우선순위 Exactly matches 긴급`
4. Path A: `Google Sheets - Create Spreadsheet Row` + `Gmail - Send Email`
5. Path B 규칙: `우선순위 Exactly matches 일반`
6. Path B에도 동일 Action 구성
7. 동일 테스트 데이터를 넣어 두 Path 실행 확인

### Zapier 필드 매핑 규격

Make와 동일하게 Trigger의 `접수시간/요청자/제목/우선순위/내용`을 각각 목적지 Google Sheets 열과 Gmail Body에 매핑했다. 따라서 도구는 달라도 **입력값과 최종 산출물의 스키마가 동일**하다.

## 2.7 단계별 샘플 입력/출력 Payload

### Trigger 입력 예시

```json
{
  "접수시간": "2026-08-14 12:00",
  "요청자": "이충훈",
  "제목": "MES 서버 접속 장애",
  "우선순위": "긴급",
  "내용": "MES 서버에 접속할 수 없어 즉시 확인 필요"
}
```

### Router / Paths 판정 결과

```text
우선순위 == "긴급" → 긴급 경로 통과
우선순위 == "일반" → 일반 경로 통과
```

### Action 1 출력 예시 - Google Sheets

```json
{
  "destination_sheet": "긴급처리",
  "row": {
    "요청자": "이충훈",
    "제목": "MES 서버 접속 장애",
    "우선순위": "긴급"
  }
}
```

### Action 2 출력 예시 - Gmail

```text
Subject: [긴급 업무요청] MES 서버 접속 장애
Body: 요청자=이충훈 / 우선순위=긴급 / 내용=MES 서버에 접속할 수 없어 즉시 확인 필요
```

### 흐름 예시

`요청접수 새 행` → Trigger가 1건을 읽음 → `우선순위=긴급` 판정 → 긴급처리 시트에 동일 데이터 저장 → Gmail로 긴급 알림 발송. 이 흐름은 Make와 Zapier에서 동일하다.

## 2.8 테스트 케이스

| TC | 입력 데이터 | 예상 분기 | 예상 결과 |
|---|---|---|---|
| TC-01 | MES 서버 접속 장애 / 우선순위: 긴급 | 긴급 | 긴급처리 시트 저장 + 긴급 이메일 발송 |
| TC-02 | 라벨 프린터 점검 / 우선순위: 일반 | 일반 | 일반처리 시트 저장 + 일반 이메일 발송 |

## 2.9 Make vs Zapier 비교 분석 - 근거 포함

> 아래 플랜 정보는 **2026-08-14 공식 페이지 확인 기준**이다.

| 비교 항목 | Make | Zapier | 근거 / 업무 영향 |
|---|---|---|---|
| UI/UX | 노드 기반 시각적 Canvas | 단계형 편집 UI | 분기 수가 늘수록 Make는 전체 구조 파악이 쉽고, Zapier는 한 단계씩 설정하기 쉬움 |
| 조건 분기 | Router + Filter | Paths | **두 결과를 모두 처리해야 하므로** 단순 Filter보다 다중 경로 기능이 적합 |
| 설정 난이도 | 모듈·번들·매핑 개념 학습 필요 | 단계형이라 초기 설정이 직관적 | 초보자는 Zapier, 복합 흐름은 Make가 유지보수에 유리 |
| 무료 플랜 | Free: 최대 **1,000 credits/월**, Routers & filters, 최소 15분 실행 간격 | Free: **100 tasks/월**, 2-step Zap; Paths는 Professional 이상 | 이번 과제처럼 분기 + Action 2개가 필요한 경우 Make Free가 직접 구현에 유리 |
| 실행 로그 | Scenario History에서 모듈별 입력/출력 추적 | Zap History에서 단계별 성공/실패 추적 | 오류 지점을 어느 Action에서 발생했는지 역추적 가능 |
| 비용 단위 | 일반적으로 실행된 모듈 Action이 credit 사용, Router는 credit 미사용 | 성공한 Action step이 task 사용, Trigger/Filter는 일반적으로 task 미사용 | Action이 늘수록 양쪽 모두 사용량이 증가하므로 단계 수 최적화 필요 |
| 협업/디버깅 | Canvas로 분기와 연결 관계 공유가 쉬움 | 순차 Step 단위 검토가 쉬움 | 인수인계 시 Make는 구조 설명, Zapier는 단계별 체크리스트에 유리 |
| 확장성 | 여러 분기·변환·오류처리 연결에 유리 | 단순/중간 복잡도 앱 연결이 빠름 | 분기 3개 이상, 공통 전처리 증가 시 Make 쪽이 구조적으로 관리하기 쉬움 |

### 비용 영향 예시

- **프로젝트 1 한 건의 핵심 업무 처리:** 목적지 시트 저장 1회 + Gmail 발송 1회 = **Action 2개**.
- Zapier는 성공한 Action step이 task로 집계되므로 이 구조는 **업무 요청 1건당 기본 2개 Action 실행**이 발생한다.
- Make에서도 Watch/Action 모듈이 실행될 때 credit가 사용되므로, 처리량이 늘면 **실행 주기와 불필요한 중간 모듈을 줄이는 것**이 중요하다.
- 단순 산술 예시로 **1,000건**을 처리하면 두 도구 모두 Action 수가 그대로 누적되므로 무료 범위를 넘어갈 수 있다. 따라서 운영 전 예상 월간 건수 × Action 수로 사용량을 산정해야 한다.

## 2.10 Filter와 Router/Paths 선택 근거

- **Filter만 사용**하면 조건이 맞지 않는 데이터는 중단되기 때문에 `긴급/일반` 두 결과를 한 워크플로우에서 모두 처리하기 어렵다.
- **Router/Paths**는 서로 배타적인 두 결과를 각기 다른 Action으로 보낼 수 있어 이번 요구사항에 적합하다.
- 현재는 분기 2개라 관리가 단순하지만 분기가 3~5개로 늘어도 경로별 책임이 분리되어 유지보수하기 쉽다.
- 데이터량이 커질수록 공통 전처리는 Router 이전에 한 번만 처리하고, 분기 후에는 각 경로에 필요한 Action만 배치해 중복 실행을 줄이는 방식이 효율적이다.

---

# 3. 프로젝트 2 - 자유 주제 자동화 설계 및 구현

## 3.1 자유 주제

**Google Drive 파일 자동 분류 및 이력 기록**

업무 중 Google Drive에 업로드되는 PDF 문서와 이미지 파일을 사람이 직접 확인하여 폴더별로 이동하고 분류 이력을 기록하는 반복 작업을 자동화하였다.

`01_업로드` 폴더에 새 파일이 등록되면 Make가 파일명을 확인하여 확장자에 따라 **PDF / 이미지** 경로로 분기하고 각각 `02_PDF`, `03_이미지` 폴더로 이동한다. 이후 파일명, 파일종류, 파일ID, 이동폴더를 Google Sheets에 자동 기록한다.

## 3.2 도구 선정 및 이유 - 수치/사례 근거

**선정 도구: Make**

1. 프로젝트 2는 **Trigger 1 + Router 1 + 각 경로 Action 2** 구조로, 복수 경로를 한 Canvas에서 관리해야 한다. Make는 Free 플랜에서도 Router/Filter를 제공한다.
2. 2026-08-14 기준 Make Free는 **최대 1,000 credits/월**이고 Router 자체는 credit 미사용이다. 과제 수준의 PDF 1건 + PNG 1건 테스트는 충분히 소규모 범위다.
3. Zapier Free는 **100 tasks/월 + 2-step Zap**이고 Paths는 Professional 이상이므로, 프로젝트 2의 분기형 워크플로우를 무료 범위에서 재현하기에는 Make가 더 적합하다.
4. 프로젝트 1에서 Make를 사용하며 Google 연결을 이미 구성했기 때문에 Google Drive + Google Sheets 연동을 추가할 때 **연결 관리와 구현 학습비용을 줄일 수 있었다.**

## 3.3 워크플로우 설계

![프로젝트 2 워크플로우](assets/project2-workflow.png)

| 단계 | 구성 | 동작 |
|---|---|---|
| 1 | Trigger | Google Drive - `01_업로드` 폴더에 새 파일이 추가되면 자동 실행 |
| 2 | Router | 파일 확장자를 기준으로 PDF / 이미지 경로 분기 |
| 3-A | PDF Action 1 | PDF 파일을 `02_PDF` 폴더로 이동 |
| 4-A | PDF Action 2 | Google Sheets에 PDF 처리 정보 기록 |
| 3-B | 이미지 Action 1 | 이미지 파일을 `03_이미지` 폴더로 이동 |
| 4-B | 이미지 Action 2 | Google Sheets에 이미지 처리 정보 기록 |

## 3.4 프로젝트 2 구성 화면 + 실행 화면 직접 증빙

### 구성 화면 - Make 전체 Scenario

![프로젝트 2 Make 전체 시나리오](assets/project2-make-scenario.png)

### 실행 화면 - PDF 경로

![PDF Filter 실행 결과](assets/project2-pdf-run.png)

### 실행 화면 - 이미지 경로

![이미지 Filter 실행 결과](assets/project2-image-run.png)

### 최종 출력 - Google Drive 이동 결과

![Google Drive 이미지 폴더 이동 결과](assets/project2-image-folder.png)

### 최종 출력 - Google Sheets 분류이력

![Google Sheets 분류이력 저장 결과](assets/project2-sheet-log.png)

## 3.5 데이터 구조 및 Action 필드 매핑

| Trigger 출력 | Move a File/Folder | Google Sheets `분류이력` |
|---|---|---|
| `ID` | **File ID**에 매핑 | `파일ID`에 기록 |
| `Name` | Filter의 파일명 판정에 사용 | `파일명`에 기록 |
| `.pdf` 판정 | 목적지 `02_PDF` | `파일종류=pdf`, `이동폴더=02_PDF` |
| `.png` 판정 | 목적지 `03_이미지` | `파일종류=png`, `이동폴더=03_이미지` |

> `Move a File/Folder`의 File ID에는 `Watch Files in a Folder` 모듈 전체 bundle이 아니라 **Trigger가 반환한 파일의 실제 `ID` 값**을 매핑한다.

## 3.6 단계별 샘플 입력/출력 Payload

### Trigger - PDF 입력 예시

```json
{
  "id": "1vljzXH69lu_0RtZ1sK8mqCfmcMXLhwe",
  "name": "SmartFlow_Factory_GenAI_Storyboa.pdf",
  "sourceFolder": "01_업로드"
}
```

### Router 판정

```text
name Ends with ".pdf" → PDF 경로
name Ends with ".png" → 이미지 경로
```

### Action 1 - 파일 이동 출력 예시

```json
{
  "fileName": "SmartFlow_Factory_GenAI_Storyboa.pdf",
  "movedTo": "02_PDF"
}
```

### Action 2 - Sheets 기록 출력 예시

```json
{
  "파일명": "SmartFlow_Factory_GenAI_Storyboa.pdf",
  "파일종류": "pdf",
  "파일ID": "1vljzXH69lu_0RtZ1sK8mqCfmcMXLhwe",
  "이동폴더": "02_PDF"
}
```

## 3.7 구현 순서

1. Google Drive에 `프로젝트분류` 폴더 생성
2. 하위 폴더 `01_업로드`, `02_PDF`, `03_이미지` 생성
3. Google Sheets에 `프로젝트2_파일분류이력` 생성
4. Make에서 `Google Drive - Watch Files in a Folder` Trigger 추가
5. `Flow Control - Router` 추가
6. PDF Filter: 파일명 `Ends with .pdf`
7. 이미지 Filter: 파일명 `Ends with .png`
8. PDF 경로: `Move a File/Folder` → `02_PDF` → `Google Sheets - Add a Row`
9. 이미지 경로: `Move a File/Folder` → `03_이미지` → `Google Sheets - Add a Row`
10. PDF 1건과 PNG 1건을 실제 업로드해 두 경로 실행 확인

## 3.8 테스트 케이스

| TC | 입력 파일 | 분기 조건 | 실제/예상 결과 |
|---|---|---|---|
| TC-01 | `SmartFlow_Factory_GenAI_Storyboa.pdf` | `.pdf` | `02_PDF` 이동 + `pdf` 이력 기록 |
| TC-02 | `화면 캡처 2026-08-14 013303.png` | `.png` | `03_이미지` 이동 + `png` 이력 기록 |

---

# 4. 실행 모니터링·재시도·대체경로 설계

> 이 절은 **운영 안정성을 위한 확장 설계안**이다. 현재 필수 과제 구현에서 모든 오류 처리 모듈을 실제 추가했다고 주장하지 않으며, 운영 적용 시 아래 정책을 적용하도록 설계하였다.

## 4.1 모니터링 항목

| 지표 | 확인 위치 | 정상 기준 | 이상 판단 예시 |
|---|---|---|---|
| 마지막 실행 시각 | Make Scenario History / Zap History | Trigger 주기 내 실행 | 30분 이상 새 실행 없음 |
| 실행 상태 | History | Success | Error / Warning 발생 |
| 분기 결과 | Route/Path 기록 | 입력값과 일치 | `긴급`이 일반 경로로 이동 |
| Action 성공 | 모듈별 결과 | Sheets/Gmail/Drive 모두 성공 | 중간 Action 실패 |
| 처리 ID | 요청 제목 또는 Drive File ID | 1회 처리 | 동일 ID 중복 처리 |
| 사용량 | Make credits / Zapier tasks | 월 한도 내 | 75~90% 이상 접근 시 점검 |

### 운영 알림 기준

- 오류가 **1회 발생**하면 History에서 원인 확인.
- 동일 오류가 **연속 3회** 발생하면 시나리오를 중지하거나 담당자 알림.
- 인증 오류(401/403 등)는 반복 재시도보다 **연결 재인증**을 우선한다.

## 4.2 재시도 정책

프로젝트 2 Make 운영 시 일시적인 네트워크/외부 서비스 장애에 대해서는 다음 정책을 적용한다.

```text
1차 실패 → 5분 후 재시도
2차 실패 → 10분 후 재시도
3차 실패 → 15분 후 재시도
3회 실패 → Incomplete Execution/실패 큐로 보관 + 담당자 확인
```

Make에는 Retry error handler와 Incomplete executions 기능이 있으며, ConnectionError/일부 Timeout 상황에는 지연 간격을 늘리는 자동 재시도 전략도 제공된다. 운영 시 오류 유형에 따라 Retry 또는 수동 재처리를 구분한다.

## 4.3 실패 시 대체 경로(Fallback) 설계

### 프로젝트 1

```text
Sheets 저장 실패
  → 오류 내용을 `자동화_실패임시저장` 시트에 기록
  → 요청자/제목/오류시각 보존
  → 복구 후 재실행
```

### 프로젝트 2

```text
파일 이동 실패
  → 원본 파일은 01_업로드에 유지
  → `분류실패` 시트에 File ID, 파일명, 오류메시지 기록
  → 담당자 알림
  → 재시도 성공 시 정상 목적지 이동
```

이렇게 하면 Action 중간 실패가 발생해도 원본 데이터가 사라지지 않고 **재처리 가능한 상태**를 유지할 수 있다.

---

# 5. 복잡성 증가 시 모듈화·재사용 설계

## 5.1 단계 분리 원칙

워크플로우가 커지면 하나의 Scenario에 모든 기능을 계속 붙이지 않고 다음 책임으로 분리한다.

1. **Ingest** - Trigger와 원본 데이터 수집
2. **Normalize** - 필드명·확장자·텍스트 형식 표준화
3. **Classify** - Router/조건 판정
4. **Execute** - 실제 이동·기록·메일 Action
5. **Audit** - 실행 로그/이력 기록
6. **Error Handling** - 재시도·실패 큐·알림

## 5.2 서브시나리오 분리 기준

다음 조건 중 하나를 만족하면 별도 Scenario/공통 모듈로 분리한다.

- 분기 경로가 **3개 이상**으로 증가
- 전체 모듈이 **8~10개 이상**이 되어 Canvas 가독성이 떨어짐
- 동일한 이메일/로그 기록 로직이 **2개 이상의 Scenario에서 반복**
- 외부 API/OCR/AI처럼 독립적으로 테스트해야 하는 처리 추가
- 실패 정책이 업무별로 달라져 각각 별도 재시도 전략이 필요

## 5.3 재사용 가능한 설계 원칙

- 폴더명, 시트명, 알림 대상은 하드코딩을 최소화하고 변수/설정값으로 관리.
- Trigger 출력은 내부 표준 스키마로 변환한 뒤 하위 단계에서 재사용.
- 공통 로깅 형식은 `실행시각 / 입력ID / 경로 / 결과 / 오류메시지`로 통일.
- 새 분기가 추가되어도 기존 Action을 복사하기보다 공통 처리 모듈을 호출하는 구조를 우선.
- 변경 전 테스트 케이스를 유지해 기존 긴급/일반, PDF/이미지 결과가 깨지지 않는지 회귀 확인.

---

# 6. 노코드 한계와 코딩 기반 확장 계획

## 6.1 이번 구현에서 확인한 노코드 한계

| 한계 | 실제 영향 |
|---|---|
| 파일명 확장자 기반 분류 | `.pdf`로 이름만 변경된 잘못된 파일을 실제 PDF로 오인할 수 있음 |
| 복잡한 중복 방지 | 동일 File ID/요청이 재실행될 때 완전한 idempotency 제어가 어려움 |
| 대량 처리 비용 | Action/모듈 수가 늘수록 credits/tasks가 증가 |
| 복잡한 데이터 가공 | 규칙이 많아지면 Router가 비대해지고 유지보수가 어려움 |
| 외부 서비스 의존 | 커넥터 변경, 인증 만료, 요금제 변경의 영향을 받음 |
| 고급 분석 | OCR, 파일 내용 검사, 복합 규칙은 별도 AI/API 연동이 필요 |

## 6.2 코딩 확장 우선순위

### P1 - 파일 실체 검증 + 중복 처리 방지

**목표:** 확장자만 보지 않고 MIME/파일 헤더를 확인하고, 동일 File ID는 1회만 처리한다.

실행 단계:

1. Make에서 파일 metadata/File ID를 Webhook 또는 HTTP로 Python API에 전달
2. Python/FastAPI에서 MIME type, 확장자, File ID 검증
3. File ID를 idempotency key로 저장
4. `{classification: "pdf", duplicate: false}` 형태로 Make에 반환
5. Make Router는 API 결과를 기준으로 최종 분기

### P2 - 오류 큐 및 관측성 강화

**목표:** 실패 원인을 구조화해 재처리 가능하게 만든다.

1. 실패 데이터를 SQLite/PostgreSQL 또는 별도 실패 시트에 적재
2. `run_id`, `input_id`, `error_type`, `retry_count`, `last_error_at` 저장
3. 재처리 API 또는 관리자 버튼으로 실패 건만 재실행
4. 성공률, 평균 처리시간, 실패유형 집계

### P3 - AI/OCR 기반 내용 분류

**목표:** 파일 확장자가 아니라 **문서 내용**을 기준으로 자동 폴더를 결정한다.

1. PDF 텍스트 추출 또는 이미지 OCR
2. LLM/분류 모델에 `계약서/매뉴얼/도면/기타` 분류 요청
3. 신뢰도 임계값 이상은 자동 이동
4. 낮은 신뢰도는 `검토필요` 폴더로 보내 사람이 확인

## 6.3 노코드 + 코드 혼합 구조

```text
Make Trigger
   ↓
Python/FastAPI 검증·분류
   ↓
Make Router
   ↓
Drive 이동 / Sheets 기록 / 알림
```

Make는 연결·오케스트레이션을 담당하고, 코드가 필요한 검증·중복방지·AI 처리는 API로 분리하면 노코드의 빠른 구축성과 코드의 정밀 제어를 함께 사용할 수 있다.

---

# 7. 협업·디버깅·비용 영향 사례

## 협업 사례

다른 팀원이 프로젝트 1을 인수할 때 README의 **입력 표준 → 분기 규칙 → 필드 매핑 → 성공 기준** 순서로 보면 특정 도구 UI를 먼저 배우지 않아도 업무 규칙을 이해할 수 있다. 이후 Make 또는 Zapier 구현 화면과 비교해 설정 차이를 확인한다.

## 디버깅 사례

`긴급 요청 메일이 오지 않음` 문제가 발생하면 다음 순서로 확인한다.

1. Trigger가 요청접수 행을 읽었는지
2. `우선순위=긴급` Filter/Path가 통과했는지
3. 긴급처리 시트 Add Row가 성공했는지
4. Gmail Action이 성공했는지
5. 실패 단계의 input/output을 History에서 확인

이 순서로 검사하면 문제 지점을 Trigger, 분기, 저장, 메일 중 하나로 좁힐 수 있다.

## 비용 영향 사례

기능 추가 시 **Action 수를 먼저 계산**한다. 예를 들어 프로젝트 1에 Slack 알림을 추가하면 기존 시트 저장 + Gmail 2개 Action에 Slack 1개가 추가되어 요청 1건당 실행 단계가 늘어난다. 처리 건수가 많을수록 task/credit 증가가 누적되므로, 공통 Action 통합과 불필요한 중복 기록 제거가 운영 비용 절감에 직접 연결된다.

---
---

# 8. 기대 효과

- 반복적인 확인 → 분류 → 기록 → 알림/이동 작업 시간 감소
- 분기 기준이 문서화되어 담당자가 바뀌어도 동일 규칙 유지
- 실행 이력과 입력/출력 스키마가 남아 오류 재현과 디버깅이 쉬움
- 실패 처리, 모듈화, 코드 확장 방향까지 정의해 단순 과제 수준을 넘어 운영 가능한 구조로 확장 가능

---

# 9. 최종 결론

프로젝트 1에서는 **동일한 입력·분기·핵심 Action·최종 출력 규격**을 Make와 Zapier 두 도구에 각각 적용하여 동일 워크플로우를 비교 구현하였다. 구성 화면, 실제 Path/Route 실행 화면, Gmail 최종 결과를 함께 제시하여 두 도구 모두 실제 동작함을 확인했다.

프로젝트 2에서는 Google Drive `01_업로드` 폴더의 새 파일을 Trigger로 받아 PDF와 이미지로 분기하고, 파일 이동과 Google Sheets 이력 기록을 자동 실행하였다. 프로젝트 1과 Trigger 유형과 최종 Action이 달라 독립적인 자유 주제 자동화를 구현하였다.

추가로 운영 관점에서 **모니터링 지표, 재시도 정책, 실패 대체경로, 서브시나리오 분리 기준, 노코드 한계와 Python/FastAPI 확장 계획**을 문서화하였다. 이를 통해 단순히 한 번 실행되는 자동화가 아니라, 반복 업무를 표준화하고 유지보수·확장 가능한 워크플로우로 설계하는 과제 목표를 충족하였다.

---
